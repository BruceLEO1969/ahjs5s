pipeline {
  agent any
  stages {
    stage('sayhi') {
      steps {
        git(changelog: true, url: 'git@github.com:BruceLEO1969/ahjs5s.git', branch: 'master', poll: true)
        echo 'Fuck it again'
      }
    }
  }
}
